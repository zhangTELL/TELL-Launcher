# TELL 启动器 PRD

## 文档信息

- 项目名称：TELL 启动器（暂定）
- 文档版本：v0.1
- 创建日期：2026-08-05
- 文档状态：评审稿
- 使用平台：Windows

## 1. 背景与目标

为个人使用制作一个 Windows 应用启动器，将常用的开发工具、AI 工具和游戏集中在一个独立窗口中，点击即可启动。

目标：

1. 把常用办公软件按 IDE 和 AI 工具分组展示。
2. 自动读取桌面游戏快捷方式，作为游戏分组的数据来源。
3. 点击任意应用卡片即可直接打开对应程序。
4. 支持搜索、手动添加、删除、排序和重新指定程序位置。
5. 所有数据保存在本地，不需要登录和联网。

## 2. 用户与使用场景

- 用户：本人，单人使用，无多账号需求。
- 使用场景：打开电脑后快速启动常用 IDE、AI 工具和游戏，不需要逐个找桌面图标或开始菜单。

## 3. 范围

### 3.1 本期范围

- 独立窗口启动器，包含办公和游戏两大分类。
- 办公分类包含固定应用清单，支持手动补充。
- 游戏分类自动扫描桌面快捷方式。
- 快速启动、搜索、添加、编辑、排序、隐藏、重新扫描。
- 本地配置保存与读取。

### 3.2 非目标

- 不做账号系统、云同步和联网功能。
- 不提供软件卸载能力。
- 不管理 Windows 开机启动项。
- 不自动扫描全盘已安装软件，办公应用以本次确认清单为准。
- 不要求兼容非 Windows 平台。

## 4. 应用清单

### 4.1 办公应用

办公分类下分两个小组：

#### IDE

| 显示名称 | 说明 |
| --- | --- |
| Visual Studio | 用户表述为“vs” |
| VS Code | |
| PyCharm | |
| IntelliJ IDEA | 用户表述为“IDEA” |

#### AI 工具

| 显示名称 | 说明 |
| --- | --- |
| Trae | 用户表述为“Trea”，以实际安装程序为准 |
| WorkBuddy | |
| ChatGPT | |
| Claude | |
| CC Switch | |
| Marvis | 用户表述为“marivs”，以实际安装程序为准 |

说明：如果某个程序名称与实际安装不一致，启动器应以实际找到的程序为准；找不到时允许用户手动指定程序文件。

### 4.2 游戏

- 数据来源：桌面快捷方式，包括当前用户桌面和公共桌面。
- 扫描范围为 `.lnk` 快捷方式。
- 扫描结果默认全部显示在“游戏”分类，用户可隐藏不需要的项目。
- 隐藏操作只影响启动器，不删除桌面快捷方式。

## 5. 页面结构

### 5.1 整体窗口

- 独立窗口，默认尺寸约 1100×700，可调整大小。
- 顶部固定区域：
  - 左侧：启动器名称。
  - 中间：搜索框。
  - 右侧：刷新、添加、管理三个图标按钮。
- 主体区域：`办公`、`游戏` 两个标签页。
- 底部状态栏：显示当前分类的应用数量。

### 5.2 办公页

- 包含两个分区：`IDE` 和 `AI 工具`。
- 每个分区有分区标题和应用卡片网格。
- 卡片显示应用真实图标和应用名称。
- 点击卡片直接打开应用。

### 5.3 游戏页

- 以网格形式展示桌面快捷方式对应的游戏。
- 卡片显示快捷方式图标和名称。
- 点击卡片直接启动游戏。
- 页面提供重新扫描入口。

## 6. 功能需求

| 编号 | 需求 | 优先级 |
| --- | --- | --- |
| F01 | 首次启动时创建办公应用默认清单 | P0 |
| F02 | 首次启动时扫描桌面快捷方式并生成游戏列表 | P0 |
| F03 | 点击应用卡片直接启动对应程序 | P0 |
| F04 | 支持全局搜索，按名称过滤应用 | P0 |
| F05 | 支持手动添加 `.exe` 或 `.lnk`，自动识别名称和图标 | P0 |
| F06 | 支持编辑应用名称、图标、所属分组和程序路径 | P1 |
| F07 | 支持删除或隐藏应用 | P1 |
| F08 | 支持拖动排序，可调整同一分组内的顺序 | P1 |
| F09 | 支持将应用移动到其他分组 | P1 |
| F10 | 支持重新扫描桌面快捷方式，且不产生重复项 | P0 |
| F11 | 应用路径失效时保留卡片并允许重新指定 | P1 |
| F12 | 启动失败时在窗口内给出短暂提示 | P1 |
| F13 | 本地保存所有配置，重启启动器后状态不丢失 | P0 |

## 7. 交互设计

### 7.1 启动应用

- 单击卡片启动对应程序。
- 启动后启动器窗口保持打开，方便连续启动多个应用。
- 启动成功不弹窗；启动失败时在顶部显示短暂提示。

### 7.2 搜索

- 输入关键词后按名称过滤。
- 在办公页搜索时，IDE 和 AI 工具合并为同一组结果。
- 无结果时显示简洁的空状态，不展示操作引导文案。

### 7.3 右键菜单

应用卡片右键菜单包含：

- 打开
- 编辑
- 移动分组
- 移除/隐藏

游戏卡片右键菜单包含：

- 打开
- 编辑名称和图标
- 隐藏
- 立即重新扫描

### 7.4 管理模式

- 点击“管理”进入编辑状态。
- 编辑状态下，卡片显示删除入口，并支持拖动排序。
- 拖动卡片到另一个分组时，自动修改分组归属。
- 再次点击“管理”退出编辑状态。

### 7.5 添加应用

- 点击“添加”打开添加窗口。
- 用户选择 `.exe` 或 `.lnk` 文件。
- 系统自动读取名称和图标，默认填充。
- 用户选择放入 IDE、AI 工具或游戏分组。
- 确认后加入对应分组并立即显示。

### 7.6 失效应用

- 程序路径不存在时，卡片变灰但保留在原位置。
- 点击卡片后进入“重新指定程序位置”流程。
- 用户重新选择程序文件后恢复正常状态。

### 7.7 刷新与去重

- “刷新”重新扫描桌面快捷方式。
- 已存在的游戏按名称或路径去重，不生成重复卡片。
- 之前被隐藏的游戏不会被重新扫描重新显示。

## 8. 数据与本地存储

- 使用本地文件保存配置，无需服务器。
- 需要保存的数据包括：
  - 办公应用默认清单及自定义修改。
  - 手动添加的应用。
  - 游戏扫描结果。
  - 用户隐藏的应用。
  - 分组归属和排序。
  - 自定义名称、图标和程序路径。
- 数据文件放在应用可访问的本地目录，具体实现阶段确定。

## 9. 视觉与体验原则

- 界面以工具化为导向，安静、克制、信息密度高。
- 使用软件真实图标，不使用装饰性图片替代。
- 常用操作使用图标按钮，并带悬停提示。
- 卡片尺寸固定，避免名称过长导致布局跳动。
- 避免在界面中使用大段说明性文字。

## 10. 验收标准

1. 启动后能显示办公页和游戏页，应用列表与确认清单一致。
2. 游戏页能扫描并显示桌面快捷方式。
3. 点击每个可用的应用卡片，能正确打开对应程序。
4. 搜索能按名称准确过滤，包含中英文和大小写不敏感匹配。
5. 能手动添加 `.exe` 和 `.lnk` 应用。
6. 能编辑、删除、隐藏、拖动排序和移动分组。
7. 刷新扫描不产生重复游戏卡片。
8. 隐藏的游戏不会因刷新重新出现。
9. 应用路径失效时能重新指定程序位置。
10. 重启启动器后，自定义配置、排序和隐藏状态仍然保留。

## 11. 后续可扩展项

- 启动后最小化到系统托盘。
- 支持自定义背景或主题。
- 支持自定义分类。
- 支持统计应用启动次数，提供最近使用排序。
