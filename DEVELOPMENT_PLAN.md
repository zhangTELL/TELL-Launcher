# TELL 启动器开发计划

## 文档信息

- 对应文档：PRD v0.1
- 计划版本：v0.1
- 创建日期：2026-08-05
- 开发平台：Windows

## 1. 开发目标

按照 PRD 交付一个可独立运行的 Windows 启动器：

- 办公页展示 IDE 和 AI 工具。
- 游戏页自动扫描桌面快捷方式。
- 支持点击启动、搜索、添加、编辑、隐藏、排序和重新指定程序位置。
- 本地保存配置，重启后状态不丢失。

## 2. 技术选型建议

| 项目 | 建议 |
| --- | --- |
| 语言与框架 | C# + WPF + .NET 8 |
| 界面 | XAML，不引入重量级 UI 框架 |
| 启动程序 | Windows ShellExecute，兼容 `.exe` 和 `.lnk` |
| 图标提取 | Windows Shell API 提取 `.exe` / `.lnk` 关联图标 |
| 本地存储 | JSON 配置文件 |
| 配置位置 | `%LOCALAPPDATA%\TELL Launcher\config.json` |
| 打包 | 单文件可执行程序，暂不制作安装器 |

选型理由：

- WPF 是 Windows 原生桌面方案，适合做独立窗口和本地文件操作。
- 不依赖网络和账号，符合 PRD 的非目标。
- 单文件 exe 方便本人使用，不增加安装成本。

## 3. 项目结构

建议按以下结构组织：

```text
TELLLauncher/
├── Models/
│   ├── AppEntry.cs
│   ├── AppGroup.cs
│   └── LauncherConfig.cs
├── Services/
│   ├── ConfigStore.cs
│   ├── AppLocator.cs
│   ├── ShortcutScanner.cs
│   ├── IconService.cs
│   └── ProcessLauncher.cs
├── ViewModels/
│   ├── MainViewModel.cs
│   ├── OfficeViewModel.cs
│   ├── GameViewModel.cs
│   └── EditDialogViewModel.cs
├── Views/
│   ├── MainWindow.xaml
│   ├── OfficeView.xaml
│   ├── GameView.xaml
│   └── EditDialog.xaml
├── Resources/
└── Tests/
```

说明：

- `Models` 保存应用数据模型和配置结构。
- `Services` 负责配置读写、程序定位、桌面扫描、图标提取和启动。
- `ViewModels` 负责界面状态和交互逻辑。
- `Views` 只负责展示和接收操作。
- `Tests` 放可自动运行的单元测试。

## 4. 数据模型

### 4.1 应用条目

```text
AppEntry
- Id
- Name
- Path
- IconPath
- Group: IDE / AITool / Game
- Order
- IsHidden
- IsManual
- IsResolved
```

### 4.2 配置结构

```text
LauncherConfig
- Version
- OfficeApps
  - IdeApps
  - AiToolApps
- Games
- HiddenGamePaths
- LastScanTime
```

配置文件只保存需要持久化的内容：

- 办公应用默认清单及自定义修改。
- 手动添加的应用。
- 游戏扫描结果。
- 隐藏的游戏路径。
- 分组、顺序、名称、图标和程序路径。

## 5. 开发阶段

### 阶段 0：项目初始化

任务：

1. 创建 WPF 项目。
2. 建立项目目录和基础命名空间。
3. 配置单文件发布参数。

完成标志：

- 项目可以编译并打开空窗口。

### 阶段 1：数据模型与本地存储

任务：

1. 实现 `AppEntry`、`AppGroup`、`LauncherConfig`。
2. 实现 `ConfigStore` 读取和写入 JSON。
3. 首次启动生成默认配置。
4. 启动时加载配置，退出或修改后保存。

对应需求：F01、F13

完成标志：

- 修改配置后重启应用，数据仍然保留。

### 阶段 2：办公应用定位

任务：

1. 建立办公应用默认清单。
2. 自动查找 Visual Studio、VS Code、PyCharm、IntelliJ IDEA。
3. 自动查找 Trae、WorkBuddy、ChatGPT、Claude、CC Switch、Marvis。
4. 找不到的应用标记为“未定位”，不阻塞其他应用。
5. 支持用户手动指定程序文件后恢复。

对应需求：F01、F11

完成标志：

- 已安装的办公应用能自动定位并显示真实图标。
- 未安装或找不到的应用保持灰卡片状态。

### 阶段 3：桌面快捷方式扫描

任务：

1. 扫描当前用户桌面和公共桌面的 `.lnk`。
2. 解析快捷方式的目标程序和图标。
3. 按路径去重，避免重复卡片。
4. 保存隐藏状态，刷新后不恢复已隐藏游戏。

对应需求：F02、F10

完成标志：

- 首次扫描能列出桌面游戏。
- 重复刷新不会产生重复项。

### 阶段 4：主界面框架

任务：

1. 实现主窗口，默认尺寸约 1100×700，可调整大小。
2. 实现顶部标题、搜索框、刷新、添加、管理按钮。
3. 实现 `办公` / `游戏` 两个标签页。
4. 实现底部状态栏和数量显示。
5. 实现办公页的 IDE、AI 工具分区。
6. 实现游戏页的游戏网格。

对应需求：F01、F02、页面结构

完成标志：

- 窗口结构符合 PRD，页面布局稳定。

### 阶段 5：应用启动

任务：

1. 实现通过 ShellExecute 启动 `.exe` 和 `.lnk`。
2. 启动后保持启动器窗口打开。
3. 启动失败时在窗口内显示短暂提示。

对应需求：F03、F12

完成标志：

- 点击每个已定位应用能正确打开。
- 失败路径不会导致程序崩溃。

### 阶段 6：搜索

任务：

1. 按名称过滤应用。
2. 支持中英文和大小写不敏感匹配。
3. 办公页搜索时合并 IDE 和 AI 工具结果。
4. 无结果时显示简洁空状态。

对应需求：F04

完成标志：

- 输入关键词后只显示匹配应用。

### 阶段 7：管理功能

任务：

1. 实现手动添加 `.exe` 和 `.lnk`。
2. 自动识别名称和图标，并允许用户修改。
3. 实现编辑应用名称、图标、分组和程序路径。
4. 实现删除或隐藏应用。
5. 实现拖动排序。
6. 实现拖动到其他分组。
7. 实现右键菜单。

对应需求：F05、F06、F07、F08、F09

完成标志：

- 添加、编辑、删除、隐藏、排序和移动分组均可正常操作。
- 所有修改保存到本地配置。

### 阶段 8：视觉打磨与验收

任务：

1. 统一卡片尺寸和间距。
2. 按钮使用图标并带悬停提示。
3. 检查文字换行和窗口缩放表现。
4. 按 PRD 验收标准逐项测试。
5. 修复测试中发现的问题。

完成标志：

- PRD 第 10 节验收标准全部通过。

## 6. 开发顺序建议

建议按以下顺序交付：

1. 先完成阶段 0 至阶段 5，得到一个可用的 MVP。
2. MVP 可运行后，再实现阶段 6 至阶段 8。
3. 管理功能是第二阶段重点，不影响 MVP 正常启动。

## 7. 测试计划

### 7.1 自动化测试

- `ConfigStore`：配置创建、读取、写入和损坏处理。
- `ShortcutScanner`：模拟桌面目录扫描、去重、隐藏保留。
- `AppLocator`：默认清单定位和未定位状态。
- 搜索过滤：中英文、大小写、无结果。

### 7.2 手工验收

- 首次启动显示默认办公应用。
- 游戏页能扫描并显示桌面快捷方式。
- 每个已定位应用点击后能正常打开。
- 添加 `.exe` 和 `.lnk` 后能启动。
- 编辑、隐藏、删除、排序和移动分组正常。
- 刷新不产生重复游戏。
- 隐藏游戏不会重新出现。
- 失效应用能重新指定路径。
- 重启后配置和顺序保持不变。

## 8. 风险与应对

| 风险 | 应对 |
| --- | --- |
| 部分软件安装位置不标准，自动定位失败 | 提供手动选择程序文件，保留灰卡片 |
| `.lnk` 图标提取不准确 | 优先读取快捷方式图标，失败时回退到目标程序图标 |
| 拖拽排序在 WPF 中实现复杂度较高 | 先实现按钮式上移下移，再实现拖拽 |
| 桌面快捷方式包含非游戏项 | 允许隐藏，不删除原始快捷方式 |
| 配置文件损坏 | 启动时检测并备份，生成默认配置 |

## 9. 交付物

- `TELLLauncher` 可执行程序。
- 本地配置文件说明。
- `PRD.md` 和本开发计划。
- 自动化测试和手工验收记录。

## 10. 待确认项

- 技术选型如无异议，按 C# + WPF 执行。
- 默认打开标签页暂定为“办公”。
- 配置目录暂定为 `%LOCALAPPDATA%\TELL Launcher\config.json`。
